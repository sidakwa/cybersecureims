# .env.example — copy to .env and fill in values. App will throw on startup if required vars are missing.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_AZURE_TENANT_ID=
VITE_API_URL=
VITE_ENABLE_ROUTE_MESSAGING=false
